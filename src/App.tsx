import { Button } from './frameworks';

const App = (): JSX.Element => (
  <div>
    <div>
      <Button>Submit</Button>
      <Button>Cancel</Button>
      <Button>Close</Button>
    </div>
  </div>
);

export default App;
