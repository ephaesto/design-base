import { Portal as TooltipPrimitivePortal } from '@radix-ui/react-tooltip';
import { styled } from '../../../../stitches.config';

const TooltipPortal = styled(TooltipPrimitivePortal, {});
export default TooltipPortal;
