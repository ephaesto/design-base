import { Trigger as TooltipPrimitiveTrigger } from '@radix-ui/react-tooltip';
import { styled } from '../../../../stitches.config';

const TooltipTrigger = styled(TooltipPrimitiveTrigger, {});
export default TooltipTrigger;
