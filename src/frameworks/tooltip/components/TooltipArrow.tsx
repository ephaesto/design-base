import { Arrow as TooltipPrimitiveArrow } from '@radix-ui/react-tooltip';
import { styled } from '../../../../stitches.config';

const TooltipArrow = styled(TooltipPrimitiveArrow, {});
export default TooltipArrow;
