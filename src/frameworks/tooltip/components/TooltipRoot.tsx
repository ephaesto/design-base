import { Root as TooltipPrimitiveRoot } from '@radix-ui/react-tooltip';
import { styled } from '../../../../stitches.config';

const TooltipRoot = styled(TooltipPrimitiveRoot, {});
export default TooltipRoot;
