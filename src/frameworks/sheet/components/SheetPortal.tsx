import { Portal as DialogPrimitivePortal } from '@radix-ui/react-dialog';
import { styled } from '../../../../stitches.config';

const SheetPortal = styled(DialogPrimitivePortal, {});
export default SheetPortal;
