import { Title as DialogPrimitiveTitle } from '@radix-ui/react-dialog';
import { styled } from '../../../../stitches.config';

const SheetTitle = styled(DialogPrimitiveTitle, {});
export default SheetTitle;
