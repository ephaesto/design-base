import { Item as ContextMenuPrimitiveItem } from '@radix-ui/react-context-menu';
import { styled } from '../../../../stitches.config';
import { itemCss } from '../../../css-utils';

const ContextMenuItem = styled(ContextMenuPrimitiveItem, itemCss);

export default ContextMenuItem;
