import { ItemIndicator as ContextMenuPrimitiveItemIndicator } from '@radix-ui/react-context-menu';
import { styled } from '../../../../stitches.config';

const ContextMenuItemIndicator = styled(ContextMenuPrimitiveItemIndicator, {});

export default ContextMenuItemIndicator;
