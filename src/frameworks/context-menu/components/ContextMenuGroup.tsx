import { Group as ContextMenuPrimitiveGroup } from '@radix-ui/react-context-menu';
import { styled } from '../../../../stitches.config';

const ContextMenuGroup = styled(ContextMenuPrimitiveGroup, {});

export default ContextMenuGroup;
