import { Close as DialogPrimitiveClose } from '@radix-ui/react-dialog';
import { styled } from '../../../stitches.config';

const DialogClose = styled(DialogPrimitiveClose, {});

export default DialogClose;
