import { Description as DialogPrimitiveDescription } from '@radix-ui/react-dialog';
import { styled } from '../../../../stitches.config';

const DialogDescription = styled(DialogPrimitiveDescription, {});

export default DialogDescription;
