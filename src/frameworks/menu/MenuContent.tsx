import { Content as MenuPrimitiveContent } from '@radix-ui/react-menu';
import { styled } from '../../../stitches.config';
import { panelStyles } from '../../css-utils';

const MenuContent = styled(MenuPrimitiveContent, panelStyles);

export default MenuContent;
