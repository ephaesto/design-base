import { CheckboxItem as MenuPrimitiveCheckboxItem } from '@radix-ui/react-menu';
import { styled } from '../../../../../../stitches.config';
import { itemCss } from '../../../../../css-utils';

const MenuCheckboxItemRoot = styled(MenuPrimitiveCheckboxItem, itemCss);

export default MenuCheckboxItemRoot;
