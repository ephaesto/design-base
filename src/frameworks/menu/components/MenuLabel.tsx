import { Label as MenuPrimitiveLabel } from '@radix-ui/react-menu';
import { styled } from '../../../../stitches.config';
import { labelCss } from '../../../css-utils';

const MenuLabel = styled(MenuPrimitiveLabel, labelCss);
export default MenuLabel;
