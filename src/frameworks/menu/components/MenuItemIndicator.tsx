import { ItemIndicator as MenuPrimitiveItemIndicator } from '@radix-ui/react-menu';
import { styled } from '../../../../stitches.config';

const MenuItemIndicator = styled(MenuPrimitiveItemIndicator, {});

export default MenuItemIndicator;
