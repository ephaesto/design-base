import { Header as AccordionPrimitiveHeader } from '@radix-ui/react-accordion';
import { styled } from '../../../../stitches.config';

const AccordionHeader = styled(AccordionPrimitiveHeader, {
  all: 'unset',
});

export default AccordionHeader;
