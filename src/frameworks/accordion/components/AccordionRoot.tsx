import { Root as AccordionPrimitiveRoot } from '@radix-ui/react-accordion';
import { styled } from '../../../../stitches.config';

const AccordionRoot = styled(AccordionPrimitiveRoot, {});

export default AccordionRoot;
