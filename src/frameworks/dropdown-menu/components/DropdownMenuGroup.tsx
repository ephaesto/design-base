import { Group as DropdownMenuPrimitiveGroup } from '@radix-ui/react-dropdown-menu';
import { styled } from '../../../../stitches.config';

const DropdownMenuGroup = styled(DropdownMenuPrimitiveGroup, {});

export default DropdownMenuGroup;
