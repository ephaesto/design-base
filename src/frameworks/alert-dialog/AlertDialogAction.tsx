import { Action as AlertDialogPrimitiveAction } from '@radix-ui/react-alert-dialog';
import { styled } from '../../../stitches.config';

const AlertDialogAction = styled(AlertDialogPrimitiveAction, {});

export default AlertDialogAction;
