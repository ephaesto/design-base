import { Portal as PopoverPrimitivePortal } from '@radix-ui/react-popover';
import { styled } from '../../../../stitches.config';

const PopoverPortal = styled(PopoverPrimitivePortal, {});

export default PopoverPortal;
