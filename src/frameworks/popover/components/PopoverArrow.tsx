import { Arrow as PopoverPrimitiveArrow } from '@radix-ui/react-popover';
import { styled } from '../../../../stitches.config';

const PopoverArrow = styled(PopoverPrimitiveArrow, {});

export default PopoverArrow;
