import { Trigger as PopoverPrimitiveTrigger } from '@radix-ui/react-popover';
import { styled } from '../../../stitches.config';

const PopoverTrigger = styled(PopoverPrimitiveTrigger, {});
export default PopoverTrigger;
